{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome",
      "url": "http://localhost:3000", // 이 부분을 정확한 URL로 설정하세요.
      "webRoot": "${workspaceFolder}",
      "sourceMaps": true
    }
  ]
}
